---
id: TASK_0003
state: in_progress
phase: critique
created_at: 2026-08-22T04:42:10.211Z
updated_at: 2026-08-22T04:51:07.333Z
title: Done means `tests/test_engine_args.py` exists (created now, since no such file is on disk; updated in place if it is later found to exist with partial content) and its tests verify the accepted `OmniEngineArgs` parsing/validation surface in `nanovllm_omni/engine_args.py` against the locked SPEC: it asserts the dataclass parses the primary `model` field and every optional field (`enforce_eager`, `gpu_memory_utilization`, `max_num_seqs`, `max_num_batched_tokens`, `dtype`, `tensor_parallel_size`, `trust_remote_code`, `device`, `extra`) with the exact accepted defaults, round-trips the kwargs that `Omni(model, **kwargs)` / `OmniBase.__init__` forward through `OmniEngineArgs(model=model, **kwargs)`, and pins the accepted error behavior for unknown kwargs (absorbed into the `extra` dict, never rejected) plus the constructor error paths that already exist (a truthy non-dict `extra` raises `TypeError` from `dict()`). Done likewise means the file passes the repo's CI-equivalent checks from `AGENTS.md` (`ruff check`, `black --check`, `python -m pytest -m "not smoke"`) without regressing any other test, and that this acceptance slice is finished before any work on the deferred TICKET-05 feature build begins.
label: test_engine_args.py: OmniEngineArgs parsing/validation
---

## raw prompt

Verify and backfill test_engine_args.py against the accepted engine-args parsing and validation — cover required/optional fields and error paths | decisions (explicit user choices — these OVERRIDE the spec doc wherever they conflict; follow them exactly): finish the still-open acceptance before touching the deferred TICKET-05 feature build

## refined prompt

GOAL
Done means `tests/test_engine_args.py` exists (created now, since no such file is on disk; updated in place if it is later found to exist with partial content) and its tests verify the accepted `OmniEngineArgs` parsing/validation surface in `nanovllm_omni/engine_args.py` against the locked SPEC: it asserts the dataclass parses the primary `model` field and every optional field (`enforce_eager`, `gpu_memory_utilization`, `max_num_seqs`, `max_num_batched_tokens`, `dtype`, `tensor_parallel_size`, `trust_remote_code`, `device`, `extra`) with the exact accepted defaults, round-trips the kwargs that `Omni(model, **kwargs)` / `OmniBase.__init__` forward through `OmniEngineArgs(model=model, **kwargs)`, and pins the accepted error behavior for unknown kwargs (absorbed into the `extra` dict, never rejected) plus the constructor error paths that already exist (a truthy non-dict `extra` raises `TypeError` from `dict()`). Done likewise means the file passes the repo's CI-equivalent checks from `AGENTS.md` (`ruff check`, `black --check`, `python -m pytest -m "not smoke"`) without regressing any other test, and that this acceptance slice is finished before any work on the deferred TICKET-05 feature build begins.

CONSTRAINTS
- The authoritative surface is `nanovllm_omni/engine_args.py` as it exists today; the tests must match it, and `engine_args.py` must not be modified merely to make tests pass.
- Lock the exact field names/types/defaults verbatim: `model: str | None = None`; `enforce_eager: bool = False`; `gpu_memory_utilization: float = 0.9`; `max_num_seqs: int | None = None`; `max_num_batched_tokens: int | None = None`; `dtype: str | None = None`; `tensor_parallel_size: int = 1`; `trust_remote_code: bool = True`; `device: str | None = None`; `extra: dict = field(default_factory=dict)`.
- Preserve the constructor semantics tested against: `OmniEngineArgs` is a mutable (non-frozen) dataclass with a custom `__init__(self, model=None, **kwargs)`; known field kwargs are popped onto typed attributes; a user-supplied `extra` is coerced via `dict(kwargs.pop("extra", {}) or {})` (so `extra=None` becomes `{}`) and then all remaining unknown kwargs are appended via `self.extra.update(kwargs)`; an unregistered keyword never raises.
- Cover only the error paths the accepted surface actually has: (a) a truthy non-dict `extra` value (e.g. `extra="abc"`) raises `TypeError` from `dict(...)`; (b) passing an unknown keyword does NOT raise — it is stored in `extra`. Do not add `validate() -> list[str]`, type coercion, or unknown-kwarg rejection to `OmniEngineArgs` in this step; anything stricter is a surface change to escalate before implementing.
- This step owns only the engine-args parsing/validation slice. Do not import, assert on, or scaffold anything owned by other steps: no `OmniRequestOutput`/`MultimodalPayload` (step 3), no `OMNI_PIPELINES`/`register_pipeline`/`resolve_pipeline_config`/deploy YAML loading (step 4), no `PipelineRunner` (step 5), no `PipelineExecutor` or `max_concurrent`/`deploy_config_path` reads (step 6), no `/v1/chat/completions` (step 7), and no `SamplingParams` coverage beyond what step 1 already locked.
- Do not touch the deferred TICKET-05 feature build: close this acceptance slice only; nothing belonging to TICKET-05 (e.g. the `minimind_o` multi-stage `process_input` wiring that `docs/aligned_interfaces.md` assigns to TICKET-05) may be started, stubbed, or expanded here.
- No new dependencies: the test uses the package's public export and stdlib only (no torch, no model-weight loads, no third-party modules), consistent with SPEC's dependency footprint.
- Follow repo conventions: file at `tests/test_engine_args.py` under pyproject's `testpaths = ["tests"]` / `python_files = ["test_*.py"]` / `addopts = "-ra -q"`; match the existing `tests/` layout and fixture style; obey `[tool.ruff]`/`[tool.black]` line-length 100; pass `AGENTS.md`'s CI-equivalent checks including `python -m pytest -m "not smoke"`; do not regress any passing test.
- Do not modify `pyproject.toml`, `nanovllm_omni/engine_args.py`, `nanovllm_omni/__init__.py`, or any non-test file in this step unless the accepted surface itself requires it (then only after confirming with the user).

KNOWN-UNKNOWNS
- SPEC.md's testing decision for this file is explicitly either/or — "rejects unknown kwargs or stores them in extra" — while the current implementation absorbs unknowns into `extra` without raising. Confirm the tests should pin absorption (no raise, stored in `extra`), or whether the user wants rejection/raising to be the accepted behavior (which is a code change to `OmniEngineArgs`, not just a test backfill).
- OmniEngineArgs currently has no `validate()`/type-validation raising path, so "error paths" is underspecified: is coverage limited to (a) non-raising absorption of unknown kwargs and (b) `TypeError` on a truthy non-dict `extra` — or is a `validate() -> list[str]`-style error path expected on the accepted surface (SPEC's generic validation pattern), requiring a code addition?
- Import style for the test: `from nanovllm_omni import OmniEngineArgs` (root re-export, so it doubles as a public-export smoke check) vs. `from nanovllm_omni.engine_args import OmniEngineArgs` (module path used by `docs/aligned_interfaces.md`) — which should the file use?
- Constructor-seam coverage: should the file include a focused test that builds `Omni(model, enforce_eager=True)`/kwargs and asserts `omni.engine_args` carries them (the wiring SPEC user story 19 pins), or stay strictly on `OmniEngineArgs` construction and leave entry-point behavior to steps 5–6?

EXTERNAL-DEPENDENCIES

## verified tooling

ruff check nanovllm_omni/ tests/
black --check nanovllm_omni/ tests/
scripts/pre-commit
python -c "from nanovllm_omni import Omni, AsyncOmni, SamplingParams, OmniRequestOutput"
python -m pytest -m "not smoke" -v
python -m compileall -q nanovllm_omni

## research

FILES
[路由错误：bad_request - The latest version of this model is only available hosted in China and requires explicit opt in: https://opencode.ai/workspace/wrk_01KY6WEQ38Q4T17KFK531BC8QT/go]

APIS
OmniEngineArgs  dataclass (mutable, non-frozen) with custom __init__; constructed as OmniEngineArgs(model: str | None = None, **kwargs: Any); importable from nanovllm_omni (root re-export, doubles as public-export smoke check), nanovllm_omni.engine_args (module path used by docs/aligned_interfaces.md), and legacy nanovllm_omni.config (used by existing tests/test_config.py)
OmniEngineArgs.__init__  __init__(self, model: str | None = None, **kwargs: Any); sets self.model, pops known-field kwargs onto typed attrs via setattr(self, name, kwargs.pop(name, dataclass_default)), never raises on unknown kwargs
OmniEngineArgs.model  str | None = None (primary field; Omni/OmniBase forward model: str positionally)
OmniEngineArgs.enforce_eager  bool = False
OmniEngineArgs.gpu_memory_utilization  float = 0.9
OmniEngineArgs.max_num_seqs  int | None = None
OmniEngineArgs.max_num_batched_tokens  int | None = None
OmniEngineArgs.dtype  str | None = None
OmniEngineArgs.tensor_parallel_size  int = 1
OmniEngineArgs.trust_remote_code  bool = True
OmniEngineArgs.device  str | None = None
OmniEngineArgs.extra  dict[str, Any] = field(default_factory=dict); built as self.extra = dict(kwargs.pop("extra", {}) or {}) so extra=None becomes {}; all remaining unknown kwargs appended via self.extra.update(kwargs) (absorption, never rejection); a truthy non-dict extra (e.g. extra="abc") raises TypeError from dict(...)
Omni  Omni(model: str, **kwargs: Any) — root entry point class exported from nanovllm_omni; constructor seam (KNOWN-UNKNOWN #4): OmniBase.__init__(model: str, **kwargs: Any) sets self.model and self.engine_args = OmniEngineArgs(model=model, **kwargs); root import is torch-free (config_registry._load_builtin_pipelines imports the minimind pipeline module which only imports torch lazily inside stages.py), so Omni("fake-model", enforce_eager=True) builds without weights
OmniBase  OmniBase.__init__(model: str, **kwargs: Any) -> None; shared constructor for Omni/AsyncOmni exposing self.engine_args, self._executor/_bundle/_pipeline/_deploy (None until lazy resolve); exported from nanovllm_omni
ruff  (CI) ruff check nanovllm_omni/ tests/
black  (CI) black --check nanovllm_omni/ tests/  (line-length 100 per [tool.black])
pytest  (CI) python -m pytest -m "not smoke"; discovery per pyproject testpaths=["tests"], python_files=["test_*.py"], addopts="-ra -q"; markers smoke/slow exist; new file carries no marker

CONTEXT
[路由错误：bad_request - The latest version of this model is only available hosted in China and requires explicit opt in: https://opencode.ai/workspace/wrk_01KY6TSTTVR3K6ZX83D4C9DSF3/go]

VERIFIED-TOOLING
  ruff check nanovllm_omni/ tests/
  black --check nanovllm_omni/ tests/
  scripts/pre-commit
  python -c "from nanovllm_omni import Omni, AsyncOmni, SamplingParams, OmniRequestOutput"
  python -m pytest -m "not smoke" -v
  python -m compileall -q nanovllm_omni

## grill Q&A

Q1: The goal's phrase "round-trips the kwargs that Omni(model, kwargs)/OmniBase.__init__ forward through OmniEngineArgs(model=model, kwargs)" is ambiguous against KNOWN-UNKNOWN #4: does the test file include a focused constructor-seam test that actually builds Omni("fake-model", enforce_eager=True) and asserts omni.engine_args carries the kwargs (feasible since the root import is torch-free, per research), or does it stay strictly on constructing OmniEngineArgs directly with the same kwargs and leave the Omni/OmniBase entry-point seam to steps 5–6?
A1: include a focused constructor-seam test that builds Omni("fake-model", enforce_eager=True) and asserts omni.engine_args carries the forwarded kwargs (root import is torch-free per research so no weights load), alongside the direct OmniEngineArgs parso/validation tests (auto)
Q2: SPEC's testing decision for this file is the either/or "rejects unknown kwargs or stores them in extra," and while the refined goal assumes absorption, KNOWN-UNKNOWN #1 explicitly asks to confirm it: should the tests lock the current behavior (unknown kwargs absorbed into extra, no raise — engine_args.py stays untouched), or do you want unknown-kwarg rejection/raising to be the accepted surface (which is a code change to OmniEngineArgs, not just a test backfill)? The error-path assertions, and whether any non-test file changes, both hinge on this.
A2: 你觉得

## spec

GOAL
Done means `tests/test_engine_args.py` exists (created now, since no such file is on disk; updated in place if later found to exist with partial content) and its tests lock the accepted `OmniEngineArgs` parsing/validation surface in `nanovllm_omni/engine_args.py` against the locked SPEC: they assert the dataclass parses the primary `model` field and every optional field with the exact accepted defaults, round-trip the kwargs that `Omni(model, **kwargs)` / `OmniBase.__init__` forward through `OmniEngineArgs(model=model, **kwargs)`, and pin the accepted error behavior — unknown kwargs are absorbed into the `extra` dict (never rejected; the delegated Q2 "你觉得" decision resolves to absorption so `engine_args.py` stays untouched) and a truthy non-dict `extra` raises `TypeError` from `dict(...)`. Per A1 the file additionally includes a focused constructor-seam test that builds `Omni("fake-model", enforce_eager=True, ...)` and asserts `omni.engine_args` carries the forwarded kwargs (root import is torch-free per research, so no weights load). Done likewise means the file passes the repo's CI-equivalent checks from `AGENTS.md` (`ruff check`, `black --check`, `python -m pytest -m "not smoke"`) without regressing any other test, nets zero non-test-file changes, and this acceptance slice completes before any deferred TICKET-05 feature work begins.

CONSTRAINTS
- The authoritative surface is `nanovllm_omni/engine_args.py` as it exists today; the tests must match it, and `engine_args.py` must not be modified merely to make tests pass.
- Lock the exact field names/types/defaults verbatim: `model: str | None = None`; `enforce_eager: bool = False`; `gpu_memory_utilization: float = 0.9`; `max_num_seqs: int | None = None`; `max_num_batched_tokens: int | None = None`; `dtype: str | None = None`; `tensor_parallel_size: int = 1`; `trust_remote_code: bool = True`; `device: str | None = None`; `extra: dict = field(default_factory=dict)`.
- Preserve the constructor semantics tested against: `OmniEngineArgs` is a mutable (non-frozen) dataclass with a custom `__init__(self, model=None, **kwargs)`; known field kwargs are popped onto typed attributes; a user-supplied `extra` is coerced via `dict(kwargs.pop("extra", {}) or {})` (so `extra=None` becomes `{}`); all remaining unknown kwargs are appended via `self.extra.update(kwargs)`; an unregistered keyword never raises.
- Q2 decision (delegated as "你觉得"): lock the current behavior — unknown kwargs are absorbed into `extra` and never rejected; unknown-kwarg rejection/raising is NOT part of this slice. `engine_args.py` stays untouched.
- Cover only the error paths the accepted surface actually has: (a) a truthy non-dict `extra` value (e.g. `extra="abc"`) raises `TypeError` from `dict(...)`; (b) passing an unknown keyword does NOT raise — it is stored in `extra`. Do not add `validate() -> list[str]`, type coercion, or unknown-kwarg rejection to `OmniEngineArgs` in this step; anything stricter is a surface change to escalate before implementing.
- Q1 decision (A1): includes a focused constructor-seam test that builds `Omni("fake-model", enforce_eager=True, ...)` and asserts `omni.engine_args` carries the forwarded kwargs, alongside the direct `OmniEngineArgs(...)` parse/validation tests. The seam test relies on the torch-free root import (no weights, no model files).
- Import the public root re-export: `from nanovllm_omni import Omni, OmniEngineArgs` (doubles as a public-export smoke check; the module path `from nanovllm_omni.engine_args import OmniEngineArgs` also works but the root re-export is required for the seam test anyway).
- This step owns only the engine-args parsing/validation slice. Do not import, assert on, or scaffold anything owned by other steps: no `OmniRequestOutput`/`MultimodalPayload` (step 3), no `OMNI_PIPELINES`/`register_pipeline`/`resolve_pipeline_config`/deploy YAML loading (step 4), no `PipelineRunner` (step 5), no `PipelineExecutor` or `max_concurrent`/`deploy_config_path` reads (step 6), no `/v1/chat/completions` (step 7), and no `SamplingParams` coverage beyond what step 1 already locked.
- Do not touch the deferred TICKET-05 feature build: close this acceptance slice only; nothing belonging to TICKET-05 (e.g. the `minimind_o` multi-stage `process_input` wiring that `docs/aligned_interfaces.md` assigns to TICKET-05) may be started, stubbed, or expanded here.
- No new dependencies: the test uses the package's public export and stdlib only (no torch, no model-weight loads, no third-party modules), consistent with SPEC's dependency footprint.
- Follow repo conventions: file at `tests/test_engine_args.py` under pyproject's `testpaths = ["tests"]` / `python_files = ["test_*.py"]` / `addopts = "-ra -q"`; match the existing `tests/` layout and fixture style; obey `[tool.ruff]`/`[tool.black]` line-length 100; pass `AGENTS.md`'s CI-equivalent checks including `python -m pytest -m "not smoke"`; do not regress any passing test.
- Do not modify `pyproject.toml`, `nanovllm_omni/engine_args.py`, `nanovllm_omni/__init__.py`, or any non-test file in this step unless the accepted surface itself requires it (then only after confirming with the user).

ACCEPTANCE
- `tests/test_engine_args.py` exists with the standard `test_*.py` naming, no pytest marker, and passes the CI-equivalent checks without regressing any other test.
- A test asserts that `model` is parsed both by default (`None`) and when passed positionally (`OmniEngineArgs("some-model")`), and asserts each optional field parses to its locked default when absent: `enforce_eager is False`, `gpu_memory_utilization == 0.9`, `max_num_seqs is None`, `max_num_batched_tokens is None`, `dtype is None`, `tensor_parallel_size == 1`, `trust_remote_code is True`, `device is None`, and `extra == {}`.
- A test constructs `OmniEngineArgs("some-model", enforce_eager=True, gpu_memory_utilization=0.5, max_num_seqs=8, max_num_batched_tokens=64, dtype="bfloat16", tensor_parallel_size=2, trust_remote_code=False, device="cuda:0")` and asserts every kwarg landed on the corresponding typed attribute while `extra` stays `{}`, proving the kwargs round-trip through the `OmniEngineArgs(model=model, **kwargs)` forwarding seam.
- A test proves unknown kwargs never raise: `OmniEngineArgs("some-model", unknown_kw=1, another="x")` succeeds and stores them — `extra == {"unknown_kw": 1, "another": "x"}` — and that an explicit `extra=None` is coerced to `{}` with further unknown kwargs merged in via `update`.
- A test asserts the accepted error path: `OmniEngineArgs("some-model", extra="abc")` raises `TypeError` (from `dict(...)`).
- The constructor-seam test builds `Omni("fake-model", enforce_eager=True, max_num_seqs=8)` without loading weights and asserts `omni.engine_args.model == "fake-model"`, `omni.engine_args.enforce_eager is True`, and `omni.engine_args.max_num_seqs == 8`, proving kwargs forwarded through `OmniBase.__init__` land on `engine_args`.
- The file imports only `from nanovllm_omni import Omni, OmniEngineArgs` plus stdlib (`pytest`, no third-party/test-only deps), and introduces no markers, no new dependencies, and no changes to `extra` semantics beyond what the accepted surface defines.
- Repo state is clean in scope: `pyproject.toml` and all `nanovllm_omni/*.py` (in particular `engine_args.py`, `__init__.py`) are unchanged; the only deliverable change is the new `tests/test_engine_args.py`. No TICKET-05 scaffolding exists from this step.

VERIFY:
```sh
python -m pytest tests/test_engine_args.py -v
python -m pytest -m "not smoke" -v
ruff check nanovllm_omni/ tests/
black --check nanovllm_omni/ tests/
python -c "from nanovllm_omni import Omni, OmniEngineArgs; a = OmniEngineArgs('fake-model', enforce_eager=True, max_num_seqs=8, extra=None, unknown_kw=1); assert a.model == 'fake-model' and a.enforce_eager is True and a.max_num_seqs == 8 and a.extra == {'unknown_kw': 1}; o = Omni('fake-model', enforce_eager=True); assert o.engine_args.model == 'fake-model' and o.engine_args.enforce_eager is True"
```
